server:
  port: 8080
spring:
  datasource:
    # Debug URL, change in production
    url: jdbc:mysql://localhost:3306/remotehost
    username: root
#    password: password
#    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
#  servlet:
#    multipart:
#      max-file-size: 1024MB
#      max-request-size: 1124MB